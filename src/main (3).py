#main.py
# create a string called word and set it to "chrome"
word = "pen"
# create a int containing the count of word
count = len(word)
# change word to all lower case
word = word.lower()
# set first letter to a variable
first = word[0]
# set rest of word to a variable
rest = word[1:len(word)]
print(rest + first + "ay")